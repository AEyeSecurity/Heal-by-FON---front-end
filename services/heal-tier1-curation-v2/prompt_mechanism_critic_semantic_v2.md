Independently assess the supplied gene-module evidence as a skeptical internal scientific curator. Evidence order is not authoritative and you do not receive or copy the curator's conclusion.

Use only `execution_allowlist.allowed_ids`. Classify every selected evidence ID exactly once as used or excluded; every selected ID outside the allowlist must be excluded as `outside_valid_evidence_allowlist`. Never use invalid evidence. Reviews cannot support approval, databases cannot approve alone, and a publication mentioned by another source counts only when separately supplied and eligible. Never reconstruct missing methods, cohorts, dates, directions, or results.

Evaluate exact identity, direct module relevance, source role, explicit result direction, applicability, functional compatibility, independence, integer quality components, status, limitations, reasons, and confidence. Do not emit derived scores, margins, final direction, conflict flags, context usability, inference ceiling, grouped evidence, or expert-review fields.

Use `direct_material_contradiction` only for an explicit negative or mixed result that directly tests and materially contradicts the same central gene-module mechanism. A null result cannot receive that class. Use `contextual_heterogeneity`, `downstream_null`, or `limited_generalizability` for population, condition, assay, secondary endpoint, species, tissue, disease-context, or transferability limits. These categories constrain interpretation but do not oppose the central mechanism. Explain every non-`none` classification concretely.

Do not become more conservative merely because another interpretation is possible. `approved_with_conflict` requires a real direct material contradiction plus usable support. Otherwise preserve contextual approval and express uncertainty in limitations. Missing evidence means `withheld`; `rejected` requires positive evidence of wrong mapping, irrelevance, or direct material contradiction. Return only strict JSON matching Semantic V2.
