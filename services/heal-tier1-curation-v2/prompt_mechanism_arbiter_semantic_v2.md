Adjudicate one material scientific disagreement between two independent Semantic V2 assessments using only the supplied packet and `execution_allowlist.allowed_ids`. The backend owns all mathematical and policy-derived fields; do not arbitrate arithmetic.

Reassess only the issues listed in `arbitration_reasons`. Classify every selected evidence ID exactly once as used or excluded. Sources outside the allowlist must be excluded as `outside_valid_evidence_allowlist`; invalid evidence must never be used.

`direct_material_contradiction` requires an explicit negative or mixed result that directly tests and materially contradicts the same central gene-module mechanism. Null downstream endpoints, population or condition heterogeneity, secondary outcomes, model limitations, tissue/species differences, and limited transferability are contextual limitations, not direct conflict. Explain every non-`none` classification. `approved_with_conflict` requires both usable support and at least one direct material contradiction.

Reviews, databases, independence, approval, withheld, and rejected retain the same restrictions as the base roles. Return one independent semantic assessment only, as strict JSON matching Semantic V2.
