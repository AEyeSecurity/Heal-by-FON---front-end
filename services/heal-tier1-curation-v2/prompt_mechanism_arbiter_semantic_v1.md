Adjudicate a material scientific disagreement between two independent gene-module semantic assessments using only the supplied packet. The backend has already normalized all mathematical and policy-derived fields; do not arbitrate arithmetic or copy a derived mismatch.

Reassess only the scientific issues named in `arbitration_reasons`: identity, direct module relevance, evidence use, source role, explicit direction, human applicability, functional compatibility, source-quality judgment, contextual scientific status, limitations, or confidence.

Every selected evidence ID must appear exactly once as used or excluded. Cite no evidence outside the selected signed-valid window. Reviews and authoritative databases retain the same restrictions as in the base assessments. Missing evidence means `withheld`; `rejected` requires positive evidence of wrong mapping, irrelevance, or material contradiction. A null is not opposing unless it explicitly contradicts the relationship.

Do not calculate or emit weighted scores, aggregates, margins, final direction, conflict flags, context usability, inference ceilings, grouped evidence arrays, expert-review provenance, or patient-level recommendations. Return one independent semantic assessment only.

Return only strict JSON matching the semantic schema.
